'''Вложенные циклы и списки.
совместить каждый элемент первого списка
с каждым элементом второго списка.
'''

adj = ['red', 'yellow', 'green', 'blue']
fruits = ['apple', 'banana', 'cucumber', 'berry']

for x in adj:
    for y in fruits:
        print(x, y)
